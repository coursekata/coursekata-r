% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-model.R
\name{geom_model}
\alias{geom_model}
\alias{stat_model}
\title{Draw fitted and implied models with ggplot2}
\usage{
geom_model(
  mapping = NULL,
  data = NULL,
  stat = "model",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

stat_model(
  mapping = NULL,
  data = NULL,
  geom = "model",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, position, show.legend, inherit.aes}{See
\code{\link[ggplot2:geom_smooth]{ggplot2::geom_smooth()}}. \code{data} may be a data frame, a function, or a
one-sided formula.}

\item{stat}{The statistical transformation. Defaults to \code{"model"}.}

\item{...}{Fixed aesthetics and other layer parameters. For an inferred
continuous model these include \code{formula}, \code{se}, \code{n}, \code{fullrange}, \code{level},
and \code{method.args}, with the meanings used by \code{\link[ggplot2:stat_smooth]{ggplot2::stat_smooth()}}.
\code{width} controls categorical model marks. With a supplied model, \code{n}
controls the prediction grid.}

\item{model}{A fitted \code{lm} or \code{aov}, a two-sided model formula, or \code{NULL} to
draw the model implied by the mapped positions.}

\item{orientation}{Layer orientation. With a supplied \code{model}, \code{NA} and
\code{"x"} put its outcome on y; \code{"y"} puts it on x. With no \code{model}, \code{NA}
infers the orientation from a one-axis mapping.}

\item{na.rm}{If \code{FALSE}, missing values are removed with a warning. If
\code{TRUE}, they are removed silently.}

\item{geom}{The geometric object. \code{stat_model()} defaults to \code{"model"}.}
}
\value{
A ggplot2 layer.
}
\description{
\code{geom_model()} adds a model to an ordinary \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}. A supplied
\code{model} may be a fit from \code{\link[stats:lm]{stats::lm()}} or \code{\link[stats:aov]{stats::aov()}}, or a two-sided
formula that is fitted once against the layer data. With no \code{model}, the
layer draws the model implied by its mapped positions, separately in each
panel and group.
}
\details{
Empty models draw an intercept, numeric predictors draw fitted lines, and
categorical predictors draw one short mark per group. A model with one
numeric and one categorical predictor draws one line per category.

A supplied model is one fixed claim evaluated on a prediction grid built
from the layer data.

A layer cannot inspect mappings on sibling layers. For a supplied model with
more than one predictor, map the displayed predictor in the model layer
itself. Single-predictor plot mappings, including transformations, are
inherited normally. Set \code{orientation = "y"} when a supplied model's outcome
is on x. Override unrelated inherited aesthetics locally or set
\code{inherit.aes = FALSE}.
}
\examples{
fit <- lm(Thumb ~ Height, data = Fingers)
ggplot2::ggplot(Fingers, ggplot2::aes(Height, Thumb)) +
  ggplot2::geom_point() +
  geom_model(model = fit)

group_fit <- lm(Thumb ~ Sex, data = Fingers)
ggplot2::ggplot(Fingers, ggplot2::aes(Sex, Thumb)) +
  ggplot2::geom_jitter(width = 0.1) +
  geom_model(model = group_fit)

}

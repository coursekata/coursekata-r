% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gf_reduce.R
\name{gf_reduce}
\alias{gf_reduce}
\title{Add Reduction Lines to a Plot}
\usage{
gf_reduce(
  object = NULL,
  gformula = NULL,
  data = NULL,
  ...,
  model,
  linewidth = 0.2,
  xlab,
  ylab,
  title,
  subtitle,
  caption,
  geom = coursekata::GeomResid,
  stat = coursekata::StatResid,
  position = "identity",
  show.legend = NA,
  show.help = NULL,
  inherit = TRUE,
  environment = parent.frame()
)
}
\arguments{
\item{object}{A ggformula plot object, typically created with \code{gf_point()}.}

\item{gformula}{Not used. \code{gf_reduce()} measures a model, not an aesthetic
formula; a model given positionally lands here and is moved to \code{model}.}

\item{data}{Not used. The reductions are measured over the data the plot was
built from. Anything supplied here is left for ggformula and ggplot2 to
answer, exactly as it is for any other \code{gf_} layer.}

\item{...}{Additional arguments. Typically these are (a) ggplot2 aesthetics to be set with
\code{attribute = value}, such as \code{color}, \code{alpha} or \code{linetype}, (b) ggplot2 aesthetics to be
mapped with \code{attribute = ~ expression}, or (c) attributes of the layer as a whole.}

\item{model}{A model already fit by \code{\link[=lm]{lm()}} or \code{\link[=aov]{aov()}}. The plot supplies
the observations' position on the other axis; the model supplies what it
predicted for each of them. May be given positionally or as \verb{model =}. A
fit without an intercept, or one fit with weights, is refused: a reduction
is only a reduction because total, error and reduction add up, and that
identity is what an unweighted intercept guarantees. \code{\link[=gf_resid]{gf_resid()}} measures
either of those fits happily, needing no such identity.}

\item{linewidth}{The width of the reduction lines. Default is \code{0.2}. Must be named.}

\item{xlab, ylab, title, subtitle, caption}{Labels for the plot.}

\item{geom, stat, position}{Not set by the caller. A reduction is drawn by its
own geom and stat, with a jitter that holds the outcome axis still so its
segments start at the grand mean without floating off it, while jittering
the other axis exactly the points layer's own jitter did.}

\item{show.legend}{Whether this layer contributes to the legend.}

\item{show.help}{Print the layer's own help instead of drawing.}

\item{inherit}{Whether the layer inherits the plot's aesthetics. The axes and
the prediction are stated outright; everything else -- a mapped \code{color}, for
instance -- is inherited from the plot.}

\item{environment}{The environment mappings are resolved in.}
}
\value{
A ggplot object with reduction lines added.
}
\description{
Draws reduction lines from the grand mean to the values a fitted model
predicts for each observation -- the third side of the sum-of-squares
decomposition, alongside \code{\link[=gf_resid]{gf_resid()}}'s residuals and the model's own fit.
Each line runs along whichever axis the plot puts the model's outcome on,
so a model of the variable drawn on x is measured across x rather than
down y.
}
\details{
The grand mean is the model's own, \code{mean()} of the outcome column the model
was fit on, not anything read off the plot's data. On a faceted plot that
is one number for every panel: every panel is measured against the same
line, which is what makes the picture in each panel a piece of one
decomposition rather than a decomposition of its own.
}
\examples{
set.seed(1)
penguins_20 <- sample(penguins, 20)

# the reduction: how far a model's fit moves the prediction from the grand
# mean, for a regression model
flipper_model <- lm(body_mass_kg ~ flipper_length_m, data = penguins_20)
gf_point(body_mass_kg ~ flipper_length_m, data = penguins_20) \%>\%
  gf_model(flipper_model) \%>\%
  gf_reduce(flipper_model, color = "blue")

# and for a two-group model on a jitter plot
gentoo_model <- lm(body_mass_kg ~ gentoo, data = penguins_20)
gf_jitter(body_mass_kg ~ gentoo, data = penguins_20, width = .1) \%>\%
  gf_model(gentoo_model) \%>\%
  gf_reduce(gentoo_model, color = "blue")

# residual (firebrick) and reduction (blue) together decompose the fit's
# distance from the grand mean
gf_point(body_mass_kg ~ flipper_length_m, data = penguins_20) \%>\%
  gf_model(flipper_model) \%>\%
  gf_resid(flipper_model, color = "firebrick") \%>\%
  gf_reduce(flipper_model, color = "blue")
}

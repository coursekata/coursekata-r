% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_overlays.R
\name{show_mean}
\alias{show_mean}
\title{Mark a Distribution's Mean}
\usage{
show_mean(plot, color = "#E60000", linetype = "longdash", linewidth = 0.7)
}
\arguments{
\item{plot}{A plot of one distribution.}

\item{color}{Line colour. Default \code{"#E60000"}.}

\item{linetype}{Line type. Default \code{"longdash"}.}

\item{linewidth}{Line width. Default \code{0.7}.}
}
\value{
The plot, with a tagged mean line added.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Draws a vertical line at the mean of the variable a distribution is built
from, spanning the count axis. Works on any plot of one distribution --
\code{gf_histogram()}, \code{gf_dotplot()}, \code{\link[=gf_squareplot]{gf_squareplot()}}. A faceted plot gets one
line per panel, at that panel's own mean, because a facet is a region with
its own subset of the data. Each line spans the count axis of the panel it
is drawn in, so \code{scales = "free_y"} is supported and no panel is stretched
to hold another panel's line.

A plot with a variable on each axis is refused. The middle of a
two-variable plot is a model, and \code{\link[=gf_model]{gf_model()}} draws models -- an hline at
the mean of the outcome for the empty model. Averaging the x variable of a
scatterplot would draw a line nobody asked for.

Facet the plot before calling \code{show_mean()}, not after: each panel's top is
measured once, from the plot as it stands when \code{show_mean()} is called, so
a panel added later has no top to draw against and is left without a line.
}
\examples{
gf_histogram(~Thumb, data = Fingers, binwidth = 5) \%>\% show_mean()

# a facet is a region with its own subset, so each panel gets its own mean
gf_histogram(~Thumb | Sex, data = Fingers, binwidth = 5) \%>\% show_mean()
}
\seealso{
\code{\link[=show_dgp]{show_dgp()}} frames a sampling distribution with the process that
generated it; \code{\link[=gf_model]{gf_model()}} draws the mean of a two-variable plot's outcome.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-resid.R
\name{geom_resid}
\alias{geom_resid}
\alias{geom_square_resid}
\alias{geom_reduce}
\alias{geom_square_reduce}
\alias{stat_resid}
\alias{stat_reduce}
\title{Residual and reduction layers for ggplot2}
\usage{
geom_resid(
  mapping = NULL,
  data = NULL,
  stat = "resid",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  linewidth = 0.2,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_square_resid(
  mapping = NULL,
  data = NULL,
  stat = "resid",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  aspect = 4/6,
  alpha = 0.1,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_reduce(
  mapping = NULL,
  data = NULL,
  stat = "reduce",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  linewidth = 0.2,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_square_reduce(
  mapping = NULL,
  data = NULL,
  stat = "reduce",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  aspect = 4/6,
  alpha = 0.1,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

stat_resid(
  mapping = NULL,
  data = NULL,
  geom = "resid",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

stat_reduce(
  mapping = NULL,
  data = NULL,
  geom = "resid",
  position = "identity",
  ...,
  model = NULL,
  orientation = NA,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, position, show.legend, inherit.aes}{See
\code{\link[ggplot2:geom_segment]{ggplot2::geom_segment()}}. \code{data} may also be a function or one-sided
formula; predictions are added after that function has selected its rows.}

\item{stat}{The statistical transformation to use. The geom constructors use
\code{"resid"} or \code{"reduce"} by default.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}. These are usually
fixed aesthetics such as \code{colour}, \code{fill}, \code{alpha}, or \code{linetype}.}

\item{model}{A model already fit by \code{\link[stats:lm]{stats::lm()}} or \code{\link[stats:aov]{stats::aov()}}. Name this
argument in a ggplot2 call, as in \code{geom_resid(model = fit)}.}

\item{orientation}{Layer orientation. \code{NA} and \code{"x"} put the model's outcome
on y; \code{"y"} puts it on x.}

\item{linewidth}{The line width. The default is \code{0.2}.}

\item{na.rm}{If \code{FALSE}, the default, missing observations are removed with a
warning. If \code{TRUE}, they are removed silently.}

\item{aspect}{The square's aspect ratio. The default is \code{4 / 6}.}

\item{alpha}{The square's transparency. The default is \code{0.1}.}

\item{geom}{The geometric object to use. The stat constructors use
\code{"resid"} by default; use \code{"square_resid"} to draw areas.}
}
\value{
A ggplot2 layer.
}
\description{
These layers measure a fitted model directly from an ordinary \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}.
A residual runs from an observed value to the model's prediction. A reduction
runs from the model's grand mean to that prediction. The square variants draw
the same distances as areas.
}
\details{
Supply \code{orientation = "y"} when the model's outcome is mapped to x. With the
default \code{orientation = NA}, the layer follows ggplot2's usual x orientation:
x is the predictor axis and the outcome is on y. Coordinate systems such as
\code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}} are applied later and do not change this argument.

A jittered point layer and its model layer must use the same
\code{\link[ggplot2:position_jitter]{ggplot2::position_jitter()}} object with a numeric seed. The model layer keeps
the fitted endpoint fixed while moving the observed endpoint by the same
amount as its point.
}
\examples{
model <- lm(Thumb ~ Height, data = Fingers)
ggplot2::ggplot(Fingers, ggplot2::aes(Height, Thumb)) +
  ggplot2::geom_point() +
  geom_resid(model = model, colour = "firebrick")

ggplot2::ggplot(Fingers, ggplot2::aes(Height, Thumb)) +
  ggplot2::geom_point() +
  geom_square_reduce(model = model, fill = "forestgreen")

jitter <- ggplot2::position_jitter(width = 0.1, seed = 42)
group_model <- lm(Thumb ~ Sex, data = Fingers)
ggplot2::ggplot(Fingers, ggplot2::aes(Sex, Thumb)) +
  ggplot2::geom_point(position = jitter) +
  geom_resid(model = group_model, position = jitter)

}

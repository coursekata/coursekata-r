% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-cutoff.R
\name{guide_cutoff}
\alias{guide_cutoff}
\alias{GuideCutoff}
\title{Draw cutoff anchors as a measured position guide}
\format{
\code{GuideCutoff} is a \link[ggplot2:Guide]{ggplot2::Guide} object.
}
\usage{
guide_cutoff(
  value,
  label = NULL,
  side = NULL,
  call_id = 1L,
  colour = ggplot2::waiver(),
  shape = 24,
  size = 4,
  linewidth = 0.5,
  title = NULL,
  theme = NULL,
  order = 0,
  position = ggplot2::waiver()
)

GuideCutoff
}
\arguments{
\item{value}{One or two finite numeric cutoff values in the position scale's
data space.}

\item{label}{Optional labels parallel to \code{value}.}

\item{side}{Optional side names parallel to \code{value}, each either \code{"lower"}
or \code{"upper"}. Supply \code{label} and \code{side} together.}

\item{call_id}{One positive integer identifying this guide instance.}

\item{colour}{Colour of the markers, leaders, and labels.}

\item{shape}{Marker shape. The default triangle points toward the panel.}

\item{size}{Marker size.}

\item{linewidth}{Width of marker outlines and leaders.}

\item{title}{Optional guide title.}

\item{theme}{A theme for this guide, or \code{NULL}.}

\item{order}{Guide order.}

\item{position}{Guide position. The default is derived from the scale.}
}
\value{
A ggplot2 guide.
}
\description{
\code{guide_cutoff()} draws one truthful marker rail and, when requested, one
measured label lane per helper call. Two-sided labels dodge along that lane
while their leaders remain tied to the truthful cutoff anchors. The guide
receives already-computed cutoff values; it never inspects the distribution
or recomputes a statistic.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-dist-mean.R
\name{StatDistMean}
\alias{StatDistMean}
\alias{stat_dist_mean}
\title{Compute a distribution's mean, per panel}
\format{
\code{StatDistMean} is a \link[ggplot2:Stat]{ggplot2::Stat} object.
}
\usage{
StatDistMean

stat_dist_mean(
  mapping = NULL,
  data = NULL,
  geom = "vline",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{The data to be displayed in this layer.}

\item{geom}{The geometric object used to display the data. Defaults to
\code{"vline"}.}

\item{position}{A position adjustment. Defaults to \code{"identity"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a
warning. If \code{TRUE}, missing values are silently removed.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, override the default aesthetics rather than
combining with them.}
}
\value{
\code{stat_dist_mean()} returns a ggplot2 layer.
}
\description{
\code{StatDistMean} receives the rows that ggplot2 has assigned to each panel and
emits their mean as an \code{xintercept}. Because ggplot2 transforms position
scales and applies hard-limit out-of-bounds handling before a stat runs, the
result follows that lifecycle: a transformed scale changes the values being
averaged, hard scale limits can remove values, and coordinate zoom does not
change the result.
}
\details{
\code{stat_dist_mean()} is the conventional layer constructor. It computes from
\code{x} and defaults to a vertical line; \code{\link[=show_mean]{show_mean()}} is the teaching-oriented
helper that resolves a distribution plot's mapping and styles this layer.
}
\seealso{
\code{\link[=show_mean]{show_mean()}} and \code{\link[=gf_model]{gf_model()}}.
}

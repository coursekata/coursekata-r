% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_cutoffs.R
\name{show_cutoffs}
\alias{show_cutoffs}
\title{Add Cutoff Markers to a Distribution}
\usage{
show_cutoffs(
  object = NULL,
  part,
  color = "#1e3a8a",
  size = 4,
  show_labels = FALSE,
  plot = lifecycle::deprecated(),
  labels = lifecycle::deprecated()
)
}
\arguments{
\item{object}{A ggplot of one distribution -- a histogram, bar chart, density,
dotplot, or \code{\link[=gf_squareplot]{gf_squareplot()}}.}

\item{part}{A distribution part, e.g. \code{middle(Thumb, .95)}. Optional: without
it, the part is read off the plot's fill aesthetic.}

\item{color}{Marker/line color. Default \code{"#1e3a8a"}.}

\item{size}{Marker size. Default \code{4}.}

\item{show_labels}{Whether to annotate the cutoffs. Default \code{FALSE}.}

\item{plot}{Deprecated alias for \code{object}.}

\item{labels}{Deprecated alias for \code{show_labels}.}
}
\value{
A ggplot object with cutoff markers and optional labels.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Adds empirical quantile cutoffs for a distribution part -- \code{middle()},
\code{tails()}, \code{upper()}, \code{lower()}, or \code{outer()}. Short dashed stems end in
downward triangles that touch the numeric axis. Optional callouts are drawn
in the data panel. Repeated labelled calls are measured and placed together.
Their boxes stay separate when the panel has room, and their routed leaders
remain tied to the exact cutoffs.

By default the part is read off the plot's fill aesthetic, e.g.
\code{fill = ~middle(Thumb, .95)}. Passing \code{part} overrides that reading: it marks
whatever part is named there instead, and the fill (if any) is ignored --
marking the 99\% cutoffs on a plot shaded for the 95\% is a deliberate, lossless
override, not a mismatch.

\code{show_cutoffs()} refuses a plot whose first layer does not draw a distribution
(a scatterplot, for instance) and, when \code{part} is given explicitly, a \code{part}
that names a variable other than the one the plot puts on x.
}
\examples{
gf_histogram(~Thumb, data = Fingers, binwidth = 5, fill = ~middle(Thumb, .95)) \%>\%
  show_cutoffs(show_labels = TRUE)

# an explicit part overrides the fill instead of requiring it to match
gf_histogram(~Thumb, data = Fingers, binwidth = 5, fill = ~middle(Thumb, .95)) \%>\%
  show_cutoffs(middle(Thumb, .99))
}
\seealso{
\code{\link[=stat_cutoff]{stat_cutoff()}} computes the same rule per panel; \code{\link[=geom_cutoff]{geom_cutoff()}}
is the lower-level drawing component. \code{\link[=guide_cutoff]{guide_cutoff()}} can place cutoff
anchors in a position guide instead.
}

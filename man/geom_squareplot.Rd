% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-squareplot.R
\name{geom_squareplot}
\alias{geom_squareplot}
\alias{stat_squareplot}
\title{Draw a countable squareplot layer}
\usage{
geom_squareplot(
  mapping = NULL,
  data = NULL,
  stat = "squareplot",
  position = "identity",
  ...,
  binwidth = NULL,
  bins = NULL,
  center = NULL,
  boundary = NULL,
  closed = NULL,
  breaks = NULL,
  bars = "none",
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE
)

stat_squareplot(
  mapping = NULL,
  data = NULL,
  geom = "squareplot",
  position = "identity",
  ...,
  binwidth = NULL,
  bins = NULL,
  center = NULL,
  boundary = NULL,
  closed = NULL,
  breaks = NULL,
  bars = "none",
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{A data frame for this layer, or \code{NULL} to inherit the plot's
data. Function-valued and formula-valued layer data are not supported.}

\item{stat}{The statistical transformation used by \code{geom_squareplot()}.}

\item{position, show.legend, inherit.aes}{See \code{\link[ggplot2:geom_histogram]{ggplot2::geom_histogram()}}.}

\item{...}{Fixed aesthetics and other layer parameters. These include \code{pad}
from \code{\link[ggplot2:stat_bin]{ggplot2::stat_bin()}}. \code{bar_color} and \code{bar_linewidth} control the bar
drawn by \code{bars = "outline"} or \code{bars = "solid"}.}

\item{binwidth, bins, center, boundary, closed, breaks}{Binning parameters with
the meanings used by \code{\link[ggplot2:stat_bin]{ggplot2::stat_bin()}}. They have no effect on a
discrete x, which is counted instead.}

\item{bars}{Display style: \code{"none"} draws squares, \code{"outline"} frames them
with their bars, and \code{"solid"} draws the bars alone.}

\item{na.rm}{Must be \code{TRUE}. A missing value has no square to draw.}

\item{geom}{The geometric object used by \code{stat_squareplot()}.}
}
\value{
A ggplot2 layer.
}
\description{
\code{geom_squareplot()} and \code{stat_squareplot()} expose \code{\link[=gf_squareplot]{gf_squareplot()}} through
ggplot2 syntax. With the default \code{bars = "none"}, each observation is drawn
as one square. Continuous x values use \code{ggplot2::stat_bin()}'s binning rules;
factors, characters, and logicals use \code{ggplot2::stat_count()}'s columns.
Both constructors retain \code{gf_squareplot()}'s defaults, warnings, factor-level
handling, and count scale.
}
\details{
A squareplot is x-only, as it is through \code{gf_squareplot()}. Map the
distribution to x. If the plot already maps y for another layer, set
\code{inherit.aes = FALSE} and supply this layer's x mapping directly.
}
\examples{
ggplot2::ggplot(Fingers, ggplot2::aes(Thumb)) +
  geom_squareplot()

ratings <- data.frame(rating = factor(c(1, 1, 3), levels = 1:5))
ggplot2::ggplot(ratings, ggplot2::aes(rating)) +
  stat_squareplot()

}
\seealso{
\code{\link[=gf_squareplot]{gf_squareplot()}}
}

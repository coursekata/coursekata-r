% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gf_lm.R
\name{gf_lm}
\alias{gf_lm}
\title{Add a Linear Model Layer, with Categorical and Shuffle Support}
\usage{
gf_lm(
  object = NULL,
  gformula = NULL,
  data = NULL,
  ...,
  width = 0.4,
  color = "#663abe",
  linewidth = 1
)
}
\arguments{
\item{object}{A formula (standalone use) or an existing ggplot object (piped
use).}

\item{gformula}{A formula specifying the plot, passed through to ggformula.}

\item{data}{A data frame (standalone use).}

\item{...}{Additional arguments passed to \code{\link[ggformula:gf_smooth]{ggformula::gf_lm()}} (continuous x)
or \code{geom_segment()} (categorical x).}

\item{width}{For categorical x, the width of each group-mean segment.
Default \code{0.4}.}

\item{color}{For categorical x, the segment color. Default \code{"#663abe"}.}

\item{linewidth}{For categorical x, the segment width. Default \code{1}.}
}
\value{
A ggplot object (or, for standalone use, whatever
\code{\link[ggformula:gf_smooth]{ggformula::gf_lm()}} returns).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
An extended \code{gf_lm()} that adds two capabilities on top of
\code{\link[ggformula:gf_smooth]{ggformula::gf_lm()}}:
\itemize{
\item \strong{Categorical x} -- when the x variable is a factor or character, it draws
horizontal group-mean segments (via \code{\link[=gf_lm_cat]{gf_lm_cat()}}) instead of a regression
line, so \code{gf_lm()} gives a sensible model layer for either kind of predictor.
\item \strong{Shuffle-safe continuous x} -- when the y mapping contains \code{shuffle()} (or
any computed expression), it is evaluated once and locked in, so the
regression line and the jittered dots reflect the same permutation instead
of two independent shuffles.
}

Called standalone (with a formula rather than a plot), it behaves exactly like
\code{\link[ggformula:gf_smooth]{ggformula::gf_lm()}}; the extra behavior activates only when piped onto an
existing plot. This function masks \code{\link[ggformula:gf_smooth]{ggformula::gf_lm()}} when coursekata is
attached.
}
\examples{
# Continuous x: an ordinary regression line, identical to ggformula::gf_lm().
gf_point(Thumb ~ Height, data = Fingers, alpha = .3) \%>\%
  gf_lm()

# Categorical x: horizontal group-mean segments instead of a sloped line.
gf_jitter(Tip ~ Condition, data = TipExperiment, width = .1) \%>\%
  gf_lm()

# Shuffle-safe: the line is fit to the same shuffled y the dots show, because
# the mapping is evaluated once rather than re-shuffled for the line.
set.seed(1)
gf_jitter(shuffle(Thumb) ~ Height, data = Fingers, width = .1) \%>\%
  gf_lm()
}
\seealso{
\code{\link[=gf_lm_cat]{gf_lm_cat()}} for the categorical case; \code{\link[ggformula:gf_smooth]{ggformula::gf_lm()}}, which
this extends.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-cutoff.R
\name{GeomCutoff}
\alias{GeomCutoff}
\alias{geom_cutoff}
\title{Draw short cutoff stems inside a panel}
\format{
\code{GeomCutoff} is a \link[ggplot2:Geom]{ggplot2::Geom} object.
}
\usage{
GeomCutoff

geom_cutoff(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  height = 0.2,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{The data to be displayed in this layer.}

\item{stat}{The statistical transformation to use. Defaults to
\code{"identity"}.}

\item{position}{A position adjustment. Defaults to \code{"identity"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{height}{Stem height as a fraction of the panel, from \code{0} to \code{1}.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a
warning. If \code{TRUE}, missing values are silently removed.}

\item{show.legend}{Logical. Should this layer be included in legends?}

\item{inherit.aes}{If \code{FALSE}, override the default aesthetics rather than
combining with them.}
}
\value{
\code{geom_cutoff()} returns a ggplot2 layer.
}
\description{
\code{GeomCutoff} draws a cutoff at each supplied \code{xintercept}. The statistical
position is transformed by the active coordinate system, while \code{height} is
measured as a fraction of the panel. The result is a short stem that starts
at the distribution baseline and follows \code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}} without
changing the orthogonal scale. Optional \code{label}, \code{side}, and \code{call_id}
aesthetics let a teaching helper add measured callouts without training the
count axis.
}
\details{
\code{geom_cutoff()} is the conventional layer constructor. It does not compute
cutoffs. \code{\link[=show_cutoffs]{show_cutoffs()}} supplies the optional marker and callout metadata
after it computes one whole-distribution plan.
}
